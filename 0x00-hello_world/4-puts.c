#include <stdio.h>
/**
 * main - In this program we are going to print a
 * new line using the puts function
 * Return: 0 (When it returns 0 then it will be successful)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
